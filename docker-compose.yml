services:
  qihb-quiz:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    container_name: qihb-quiz-app
# To run:
# docker-compose up -d
#
# To view logs:
# docker-compose logs -f
#
# To stop:
# docker-compose down
#
# To rebuild after code changes:
# docker-compose up -d --build
